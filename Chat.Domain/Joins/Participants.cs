namespace Chat.Domain.Joins
{
    public static class Participants
    {
        public const string ConversationId = nameof(ConversationId);
        public const string UserId = nameof(UserId);
    }
}
